<UserControl x:Class="GeneradoNominaSystem.Presentation.Views.EmpresaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Empresas" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
            <Button Content="Recargar" Command="{Binding RecargarCommand}" Margin="16,0,0,0" Padding="12,4" />
            <Button Content="Nueva" Command="{Binding NuevaCommand}" Margin="8,0,0,0" Padding="12,4" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding EmpresasLista}"
                      SelectedItem="{Binding Seleccionada}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Comercial" Binding="{Binding NombreComercial}" Width="*" />
                    <DataGridTextColumn Header="NIT" Binding="{Binding Nit}" Width="120" />
                    <DataGridCheckBoxColumn Header="Activa" Binding="{Binding Activo}" Width="60" />
                </DataGrid.Columns>
            </DataGrid>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding Edicion}">
                    <TextBlock Text="Razón social" />
                    <TextBox Text="{Binding RazonSocial, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Nombre comercial" />
                    <TextBox Text="{Binding NombreComercial, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="NIT" />
                    <TextBox Text="{Binding Nit, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Dígito verificación" />
                    <TextBox Text="{Binding DigitoVerificacion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Teléfono" />
                    <TextBox Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Email" />
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Dirección" />
                    <TextBox Text="{Binding DireccionCompleta, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Ciudad" />
                    <TextBox Text="{Binding Ciudad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Departamento" />
                    <TextBox Text="{Binding Departamento, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="País" />
                    <TextBox Text="{Binding Pais, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Régimen tributario" />
                    <TextBox Text="{Binding RegimenTributario, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Representante legal" />
                    <TextBox Text="{Binding RepresentanteLegal, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <CheckBox Content="Responsable IVA" IsChecked="{Binding ResponsableIva}" Margin="0,0,0,6" />
                    <TextBlock Text="Ruta logo" />
                    <TextBox Text="{Binding LogoRuta, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Padding="16,4" />
            <Button Content="Desactivar" Command="{Binding DesactivarCommand}" Margin="8,0,0,0" Padding="16,4" />
            <ProgressBar IsIndeterminate="True" Visibility="{Binding Ocupado, Converter={StaticResource BoolToVisibility}}" Width="100" Height="8" Margin="16,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Mensaje}" Style="{StaticResource MensajeStyle}" Margin="16,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
