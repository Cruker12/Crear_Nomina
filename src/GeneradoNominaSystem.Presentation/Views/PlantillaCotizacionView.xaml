<UserControl x:Class="GeneradoNominaSystem.Presentation.Views.PlantillaCotizacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Empresa" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding EmpresasLista}"
                      SelectedItem="{Binding EmpresaSeleccionada}"
                      DisplayMemberPath="NombreComercial"
                      Width="250" Margin="8,0,0,0" />
            <Button Content="Recargar" Command="{Binding RecargarCommand}" Margin="16,0,0,0" Padding="12,4" />
            <Button Content="Nueva" Command="{Binding NuevaCommand}" Margin="8,0,0,0" Padding="12,4" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding PlantillasLista}"
                      SelectedItem="{Binding Seleccionada}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                    <DataGridCheckBoxColumn Header="Activa" Binding="{Binding Activo}" Width="60" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" DataContext="{Binding Edicion}">
                <TextBlock Text="Nombre de la plantilla" />
                <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                <TextBlock Text="La personalización visual (logo, firma, encabezado) usa la configuración por defecto en v1." TextWrapping="Wrap" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Padding="16,4" />
            <Button Content="Desactivar" Command="{Binding DesactivarCommand}" Margin="8,0,0,0" Padding="16,4" />
            <Button Content="Activar" Command="{Binding ActivarCommand}" Margin="8,0,0,0" Padding="16,4" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" Margin="8,0,0,0" Padding="16,4" />
            <ProgressBar IsIndeterminate="True" Visibility="{Binding Ocupado, Converter={StaticResource BoolToVisibility}}" Width="100" Height="8" Margin="16,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Mensaje}" Style="{StaticResource MensajeStyle}" Margin="16,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
