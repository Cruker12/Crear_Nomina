<UserControl x:Class="GeneradoNominaSystem.Presentation.Views.EmpleadoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Empresa" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding EmpresasLista}"
                      SelectedItem="{Binding EmpresaSeleccionada}"
                      DisplayMemberPath="NombreComercial"
                      Width="250" Margin="8,0,0,0" />
            <Button Content="Recargar" Command="{Binding RecargarCommand}" Margin="16,0,0,0" Padding="12,4" />
            <Button Content="Nuevo" Command="{Binding NuevoCommand}" Margin="8,0,0,0" Padding="12,4" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding EmpleadosLista}"
                      SelectedItem="{Binding Seleccionado}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombres" Binding="{Binding Nombres}" Width="*" />
                    <DataGridTextColumn Header="Apellidos" Binding="{Binding Apellidos}" Width="*" />
                    <DataGridTextColumn Header="Documento" Binding="{Binding NumeroDocumento}" Width="110" />
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding Edicion}">
                    <TextBlock Text="Tipo documento" />
                    <ComboBox ItemsSource="{Binding DataContext.TiposDocumento, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              SelectedItem="{Binding TipoDocumento}" Margin="0,0,0,6" />
                    <TextBlock Text="Número documento" />
                    <TextBox Text="{Binding NumeroDocumento, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Nombres" />
                    <TextBox Text="{Binding Nombres, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Apellidos" />
                    <TextBox Text="{Binding Apellidos, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Cargo" />
                    <TextBox Text="{Binding Cargo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Área / Departamento" />
                    <TextBox Text="{Binding DepartamentoArea, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Tipo contrato" />
                    <ComboBox ItemsSource="{Binding DataContext.TiposContrato, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              SelectedItem="{Binding TipoContrato}" Margin="0,0,0,6" />
                    <TextBlock Text="Fecha ingreso" />
                    <DatePicker SelectedDate="{Binding FechaIngreso}" Margin="0,0,0,6" />
                    <TextBlock Text="Salario base" />
                    <TextBox Text="{Binding SalarioBaseMonto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Moneda" />
                    <TextBox Text="{Binding SalarioBaseMoneda, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Email" />
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                    <TextBlock Text="Teléfono" />
                    <TextBox Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Padding="16,4" />
            <Button Content="Desactivar" Command="{Binding DesactivarCommand}" Margin="8,0,0,0" Padding="16,4" />
            <ProgressBar IsIndeterminate="True" Visibility="{Binding Ocupado, Converter={StaticResource BoolToVisibility}}" Width="100" Height="8" Margin="16,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Mensaje}" Style="{StaticResource MensajeStyle}" Margin="16,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
